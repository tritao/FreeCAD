This group builds the user-facing Plan Edit environment on the generic and semantic foundations.

It covers session lifecycle, semantic library assets and sources, generated previews, contextual TaskView sections, context-sensitive library insertion, spaces and regions, and provider-owned workflow orchestration.

Architectural invariant:

> The Plan Edit session coordinates capabilities while object-specific behavior remains owned by semantic providers.
