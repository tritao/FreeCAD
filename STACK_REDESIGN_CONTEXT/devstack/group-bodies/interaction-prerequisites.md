This group supplies reusable interaction and build prerequisites consumed by Plan Edit.

The Draft interaction-host layer supports cancellable embedded tools, temporary snap profiles, scoped working planes, and controlled point acquisition. Qt resource dependency tracking ensures generated GUI assets rebuild reliably.

Architectural invariant:

> BIM composes generic Draft and build capabilities instead of creating a parallel input environment.
