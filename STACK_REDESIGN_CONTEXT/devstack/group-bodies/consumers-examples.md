This group proves that contextual BIM representations can serve consumers beyond the interactive Plan Edit viewer.

TechDraw projects the same semantic representation contract with explicit legacy fallback. Generated example documents and end-to-end tests exercise wall relations, contextual rendering, path ownership, and editing in realistic saved files.

Architectural invariant:

> Interactive display, documentation, and examples consume the same semantic BIM model rather than separate conversion pipelines.
